import { Dimensions } from "react-native";

export const { width } = Dimensions.get("screen");

export const POSTER_WIDTH = width * 0.15;
export const TEXT_BOX_WIDTH = width * 0.6;
export const SERVICE_ICON_SIZE = 35;
export const SERVICE_CONTAINER_WIDTH = 60;
